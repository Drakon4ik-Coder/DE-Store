# DE-Store Metrics Report

| Metric | Value | How measured (exact command/script) | Notes/assumptions |
| --- | --- | --- | --- |
| Project name | `DE-Store Prototype` (`destore`) | `sed -n '1,40p' README.md` and `sed -n '1,40p' pom.xml` | README title is `DE-Store Prototype (Service-Based / Microservice-Lite)`; root POM `name`/`artifactId` is `destore`. |
| Tech stack | `Java 17`, multi-module `Maven`, `Spring Boot 3.2.0`, `Spring Cloud Gateway 2023.0.0`, Spring Web/WebFlux, Spring Data JPA, Spring Kafka, PostgreSQL, Kafka, Docker Compose | `sed -n '1,220p' README.md`; `sed -n '1,260p' pom.xml`; `for f in gateway/pom.xml pricing-service/pom.xml inventory-service/pom.xml finance-gateway-service/pom.xml notification-worker/pom.xml loyalty-service/pom.xml reporting-service/pom.xml shared-contracts/pom.xml; do sed -n '1,220p' "$f"; done` | Only includes frameworks/products explicitly declared in README, POMs, and compose file. |
| Maven modules | `8` | `python3 - <<'PY'  # xml.etree parses pom.xml ./modules/module` | Modules: `shared-contracts`, `gateway`, `pricing-service`, `inventory-service`, `finance-gateway-service`, `notification-worker`, `loyalty-service`, `reporting-service`. |
| `docker-compose` services | `10` | `python3 - <<'PY'  # count top-level entries under services: in docker-compose.yml` | Includes infra + app services: Zookeeper, Kafka, Postgres, 6 app services, and gateway. |
| REST endpoints (controllers only) | `14 total` | `python3 - <<'PY'  # scan **/*Controller.java for @RestController + *Mapping` | Key paths: `GET /pricing/{sku}`, `POST /pricing/{sku}`, `GET /pricing`, `GET /inventory/{sku}`, `POST /inventory/upload`, `POST /finance/approve`, `POST/GET /loyalty/members`, `GET /reports/summary`, `GET /notify/ping`. Paths were normalized from controller annotations. |
| Kafka topics (`destore.topics.*`) | `4 declarations across 3 modules; 2 unique topics` | `python3 - <<'PY'  # scan **/src/main/resources/application.yml for destore.topics.*` | Unique topics are `low-stock` and `price-changed`; declarations appear in pricing, inventory, and notification-worker configs. |
| Kafka producers (`KafkaTemplate` usage) | `2 producer components` | `python3 - <<'PY'  # count files containing KafkaTemplate<` | Producer components are the pricing and inventory application services; raw text contained `4` `KafkaTemplate<` occurrences (imports/field/ctor usage included). |
| Kafka consumers (`@KafkaListener`) | `2 listener methods` | `rg -n '@KafkaListener' -g '*.java'` | Both listeners are in the notification worker. |
| Persistence entities (`@Entity`) | `3` | `rg -n '@Entity\\b' -g '*.java'` | Entities are the pricing, inventory, and loyalty domain models. |
| Repository types (`*Repository.java`) | `5` | `rg --files -g '*Repository.java'` | Counts repository interfaces and adapter classes with `Repository` suffix. |
| Java files | `31` | `rg --files -g '*.java' | wc -l` | Source scan covered all Java files in the repo. |
| Java LOC | `923` | `rg --files -g '*.java' | xargs wc -l` | `cloc` was not installed (`CLOC_NOT_FOUND`), so `wc -l` fallback was used. |
| Build time | `3.02 s` | `/usr/bin/time -f 'elapsed_seconds=%e' mvn clean install -DskipTests -Dmaven.repo.local=.m2repo > /tmp/destore_mvn_build.log` | Warm local cache in `.m2repo`; this is not a first-download build. |
| Stack time-to-green | `0.378 s` | `python3 - <<'PY'  # subprocess(['docker','compose','up','-d']) + poll 6 /actuator/health URLs until all report UP` | Warm stack was already running, so this is idempotent `docker compose up -d` convergence time, not a cold boot. |
| Seed data via gateway | `200 / 200` | `python3 - <<'PY'  # POST /pricing/resume-bench-001 and POST /inventory/upload before benchmarks` | Seeded benchmark SKU `resume-bench-001`; both POSTs returned HTTP `200`. |
| `GET /pricing/{sku}` via gateway (`:8080`) | `p50 4.938 ms, p95 8.326 ms, p99 10.256 ms, 0.0% errors, 3596.66 req/s` | `python3 - <<'PY'  # urllib.request + ThreadPoolExecutor(max_workers=20), N=1000` | Target was `http://localhost:8080/pricing/resume-bench-001`; 10 warm-up requests were excluded. |
| `GET /reports/summary` via gateway (`:8080`) | `p50 10.603 ms, p95 18.217 ms, p99 21.141 ms, 0.0% errors, 1729.85 req/s` | `python3 - <<'PY'  # urllib.request + ThreadPoolExecutor(max_workers=20), N=1000` | Target was `http://localhost:8080/reports/summary`; same warm-up and concurrency settings. |
| `GET /pricing/{sku}` direct (`:8081`) | `p50 5.053 ms, p95 11.590 ms, p99 15.152 ms, 0.0% errors, 3381.39 req/s` | `python3 - <<'PY'  # urllib.request + ThreadPoolExecutor(max_workers=20), N=1000` | Direct pricing-service baseline for gateway overhead comparison. |
| Gateway overhead on `GET /pricing/{sku}` | `p50 -0.115 ms, p95 -3.264 ms, p99 -4.896 ms, +215.27 req/s vs direct` | `python3 - <<'PY'  # compute (gateway benchmark - direct benchmark)` | Negative deltas mean no added overhead was detectable in this warm localhost run; the difference is within client/runtime noise. |
| Kafka eventing latency (`pricing-service` -> `notification-worker`) | `200/200 matched, median 0.491 ms, p95 0.657 ms` | `python3 - <<'PY'  # POST 200 unique /pricing/{sku} updates via gateway + docker compose logs --since ... --timestamps correlation` | Measured log-to-log delta from `Price set for sku=...` to `price changed for ...`; same-host Docker timestamps, not end-to-end client-observed latency. |

## Top 3 Strongest Metrics

1. `GET /pricing/{sku}` via gateway is the strongest runtime number: `3,596.66 req/s` at `p95 8.326 ms` and `0.0%` errors. CV phrasing: `Benchmarked a Spring Cloud Gateway pricing endpoint at 3.6k req/s (p95 8.3 ms, 0% errors) under 1,000-request / 20-concurrency local load.`
2. Kafka event propagation is the strongest async number: `200/200` correlated events with `0.491 ms` median and `0.657 ms` p95 log-to-log latency. CV phrasing: `Validated Kafka-based event propagation across pricing and notification services with 200/200 matched events and sub-1 ms p95 intra-stack latency in Docker Compose.`
3. The architecture breadth is the strongest static platform metric: `8` Maven modules, `10` compose services, and `14` REST endpoints. CV phrasing: `Built an 8-module, 10-service Spring Boot microservice prototype exposing 14 REST endpoints behind a centralized API gateway.`
